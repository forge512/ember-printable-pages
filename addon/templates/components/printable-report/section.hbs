{{#if (and this.shouldRender this.page)}}
  <h2>Section {{this.elementId}}</h2>
  {{#each this.items as |item index|}}
    {{#printable-report/section-item}}
      {{yield (hash
        data=item
        index=index
      )}}
    {{/printable-report/section-item}}
  {{/each}}

{{/if}}
