import Component from "@ember/component";
import layout from "../../templates/components/printable-report/section-item";

export default Component.extend({
  layout,
  classNames: ["column-break-inside-avoid"]
});
